{% extends 'base.html.twig' %}
{% block header_title %}<h1>Modifier <strong>{{ user.username }}</strong></h1>{% endblock %}
{% block body %}
   <div class="row">
      {{ form_start(form, {'action' : path('user_edit_password', {'id' : user.id})}) }}
      {{ form_widget(form) }}
      <button type="submit" name="_csrf_token" value="{{ csrf_token('EditUser') }}" class="btn btn-success pull-right">Modifier</button>
      {{ form_end(form) }}
   </div>
{% endblock %}