{% extends 'base.html.twig' %}
{% block header_title %}<h1>Créer un utilisateur</h1>{% endblock %}
{% block body %}
    <div class="row">
        {{ form_start(form, {'action' : path('user_create')}) }}
            {{ form_widget(form) }}
            <button type="submit" name="_csrf_token" value="{{ csrf_token('AddUser') }}" class="btn btn-success pull-right">Ajouter</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
